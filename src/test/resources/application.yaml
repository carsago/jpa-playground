spring:
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://localhost:13306/store?rewriteBatchedStatements=true
      username: user
      password: password
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # (1) ??? ?? ??
    properties:
      hibernate:
        jdbc:
    #          batch_size: 9000
    database-platform: org.hibernate.dialect.MySQL8Dialect

